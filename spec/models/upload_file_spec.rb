require 'spec_helper'

describe UploadFile do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    UploadFile.create!(@valid_attributes)
  end
end
