require 'spec_helper'

describe Pinyin do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Pinyin.create!(@valid_attributes)
  end
end
