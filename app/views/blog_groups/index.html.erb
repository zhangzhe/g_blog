<div class="right"><%= will_paginate @blog_groups %></div>
<br />
<%= render :partial=> "new" %>

<% @blog_groups.each do |@blog_group| %>
<%= render(:partial => "show") %> 
<% end %>

<%= will_paginate @blog_groups %>
